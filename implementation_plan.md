# 분임조 평가 대시보드 기획 및 개발 계획 (History)

## [v1.0] 2026-08-04: 프리미엄 모던 웹 애플리케이션 디자인 리뉴얼
### 목표
분임조 평가 대시보드 시스템에 "WOW" 수준의 시각적 경험을 제공하기 위해 글래스모피즘(Glassmorphism) 기반의 최신 웹 트렌드 디자인을 전면 적용합니다.

### 주요 변경 사항
- **전역 테마 및 스타일링**: 다크 레이디얼 그라데이션 배경 및 `backdrop-blur` 적용 (`index.css`, `App.tsx`)
- **컴포넌트 리뉴얼**: 
  - 카드 및 탭 요소에 마이크로 애니메이션(Hover 팝업, Glow 이펙트) 적용 (`Dashboard.tsx`, `CircleManager.tsx`)
  - 인풋 폼 및 버튼에 포커스 링 효과 및 네온 컬러 스타일링 (`EvaluatorForm.tsx`, `ExcelImporter.tsx`)
  - 1등, 2등 포상 카드에 그라데이션 광원 하이라이트 추가

---

## [v2.0] 2026-08-04: 분임조 평가 결과 PDF 보고서 출력 기능 추가

현재 대시보드 화면에 나오는 분임조별 세부 분석 결과를 공식 품의서 등에 첨부하기 좋은 **보고서 양식의 PDF 파일**로 곧바로 다운로드할 수 있는 기능을 구현합니다.

### Proposed Changes

#### 1. 라이브러리 설치
- `html2canvas`: HTML 요소를 이미지로 변환
- `jspdf`: 이미지를 기반으로 PDF 파일 생성 및 다운로드

#### 2. 컴포넌트 추가 및 수정
- **[Dashboard.tsx](file:///d:/Antigravity/Quality_Circle/src/components/Dashboard.tsx)**
  - 세부 평가 분석 모달 상단에 **[PDF 다운로드]** 버튼(Lucide `FileDown` 아이콘 활용) 추가.
  - A4 비율로 렌더링되는 인쇄 전용 템플릿(결재란 포함, 흰색 배경)을 UI 상에 보이지 않게(absolute off-screen) 구현한 뒤 캡처하도록 처리.
  - 캡처 완료 시 자동 다운로드 되도록 로직 구현 (`handleDownloadPDF`)

### Verification Plan
1. 패키지 설치 확인 및 개발 서버 구동
2. 분임조 상세 리포트의 [PDF 다운로드] 버튼 클릭
3. 다운로드된 PDF 파일에 결재란과 레이더 차트가 온전히 포함되었는지 확인

---

## [v3.0] 2026-08-04: 품의서 첨부용 종합 평가 결과 요약 장표(PDF) 구현

사용자의 피드백을 반영하여, 개별 분임조 리포트가 아닌 **전체 분임조의 평가 결과와 순위를 한눈에 볼 수 있는 '종합 요약 장표'** 형태의 깔끔한 A4 공식 문서(PDF) 다운로드 기능을 추가합니다.

### User Review Required

> [!IMPORTANT]
> **출력될 요약 장표(PDF)의 레이아웃 제안**
> 1. **문서 헤더**: 결재란 (담당/팀장/공장장) 및 공식 타이틀 ("202X년 X월 분임조 종합 평가 결과 보고서")
> 2. **종합 순위 및 포상 내역**: 1등(최우수), 2등(우수) 분임조 하이라이트 요약 텍스트
> 3. **종합 평가 테이블**: 5개 분임조의 모든 세부 항목 점수(3정5S, 회합, 테마진행, 제안 등)와 총점, 순위를 일목요연하게 표(Table) 형태로 정리
> 4. **차트 (선택)**: 표 아래에 전체 분임조 점수 비교를 위한 막대 차트(Bar Chart) 배치
> 
> 이 구성이 품의서에 첨부하시고자 하는 정식 서류 양식에 부합하는지 확인 부탁드립니다. (차트를 빼고 표만 크게 넣는 것이 좋다면 말씀해 주세요!)

### Proposed Changes

#### [MODIFY] [Dashboard.tsx](file:///d:/Antigravity/Quality_Circle/src/components/Dashboard.tsx)
- 대시보드 화면 최상단(또는 순위 목록 우측 상단)에 **[월간 종합 평가 보고서(PDF) 다운로드]** 버튼을 신규 배치합니다.
- 기존의 어두운 테마나 글래스모피즘이 전혀 들어가지 않은, 100% **인쇄용/결재용 서식에 맞춘 흑백/무채색 위주의 깔끔한 표(Table) 중심의 숨김 템플릿(`.summary-pdf-template`)**을 구성합니다.
- 이 종합 템플릿을 `html2canvas`와 `jspdf`를 통해 캡처하여 하나의 A4 문서로 다운로드되도록 로직(`handleDownloadSummaryPDF`)을 추가합니다.
