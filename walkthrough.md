# [품질분임조 평가 및 집계 시스템] 개발 및 개선 히스토리 (Walkthrough)

본 문서는 삼양이노켐 품질분임조 평가 및 집계 시스템의 주요 기능 구현, UI/UX 리뉴얼, PDF 자동 생성 엔진 개선 및 설정 기본값 조정에 대한 전체 진행 내역입니다.

---

## [v1.0] 2026-08-04: 프리미엄 디자인 리뉴얼 작업 완료

분임조 평가 대시보드 시스템에 고급스러운 시각적 경험을 제공하기 위해 **프리미엄 모던 웹 애플리케이션** 디자인 리뉴얼을 완료했습니다.

### 주요 적용 사항
1. **글래스모피즘(Glassmorphism) 기반 테마 적용**
   - 전역 배경을 깊이감 있는 **레이디얼 그라데이션**으로 변경했습니다.
   - 모든 카드 요소에 반투명 배경과 블러 효과(`backdrop-blur`)를 적용해 고급스러운 느낌을 더했습니다.
2. **마이크로 애니메이션 및 전환 효과**
   - 화면 진입 시 자연스럽게 떠오르는 **Fade-In-Up** 애니메이션을 도입했습니다.
   - 각 카드 컴포넌트, 탭 버튼, 입력 폼 영역 등에 **호버 시 팝업 및 발광(Glow) 효과**를 추가했습니다.
3. **타이포그래피 및 폼 요소(UI) 강화**
   - `Outfit`과 `Inter` 폰트 조합 및 삼양 CI 네온 컬러(인디고, 사이안, 에메랄드 등) 링(Ring) 포커스 효과 추가.

---

## [v2.0] 2026-08-04: 공식 문서용 PDF 보고서 자동 생성 기능 완료

품의서 등 공식 서류에 바로 첨부하실 수 있도록 최적화된 PDF 보고서 생성 기능을 추가했습니다.

### 주요 적용 사항
1. **결재란 자동 생성**: 출력되는 PDF의 우측 상단에 담당, 팀장, 공장장 서명이 가능한 결재란 표를 기본으로 탑재했습니다.
2. **문서 인쇄용 밝은(Light) 테마 캡처**: 화면에서는 세련된 다크 테마를 유지하지만, PDF 캡처 시 내부적으로 **문서용 흰색 배경(A4 비율)**으로 재구성된 보고서를 생성해 잉크 낭비 없는 깔끔한 결과물을 얻을 수 있습니다.
3. **즉시 다운로드 기능**: 별도의 인쇄 창을 거치지 않고, 대시보드의 세부 리포트에서 **[PDF 다운로드]** 버튼 클릭 즉시 다운로드 되도록 연동을 완료했습니다.
4. **레이아웃 통일성 확보**: 엑셀 파싱 화면과 평가 폼 화면의 좌우 너비 라인을 정확히 일치하도록 조치했습니다.

---

## [v3.0] 2026-08-04: 품의서 첨부용 종합 평가 결과 요약 장표(PDF) 추가

개별 분임조 리포트뿐만 아니라 **모든 분임조의 종합적인 실적과 순위를 한 장에 담은 요약 장표** 다운로드 기능을 개발했습니다.

### 주요 적용 사항
1. **다운로드 접근성 강화**: 대시보드 최상단 우측에 **[품의서용 종합 보고서 다운로드]** 버튼을 배치하여 쉽게 접근할 수 있습니다.
2. **공식 서류 맞춤형 템플릿**:
   - **문서 헤더**: 최상단 결재란 (담당/팀장/공장장) 및 공식 제목 배치
   - **종합 차트**: 5개 분임조의 점수를 직관적으로 비교할 수 있는 막대형 차트(Bar Chart) 삽입
   - **종합 테이블**: 전체 분임조의 순위부터 각 세부 항목 점수, 총점까지 한눈에 파악할 수 있는 표(Table) 형식의 요약표 삽입
   - **포상 내역**: 하단에 당월 1등(최우수), 2등(우수) 포상 확정 내역을 명시적으로 강조

---

## [v4.0] 2026-08-05: PDF 서식 모던 오피스 스타일 리뉴얼, 렌더링 엔진 격리 및 컬럼 비율 최적화

PDF 다운로드 시 발생하던 OKLCH 컬러 파싱 에러를 완벽 해결하고, 사용자의 요구사항에 맞춘 모던 오피스 레이아웃 리뉴얼을 완료했습니다.

### 주요 적용 사항
1. **샌드박스 `iframe` 렌더링 엔진 도입 (`oklch` 에러 근본 해결)**:
   - Tailwind CSS v4가 전역에 주입하는 `oklch()` 색상 함수로 인해 `html2canvas`가 파싱 에러를 내는 문제를 방지하기 위해, PDF 생성 전용 가상 `iframe` 샌드박스를 동적으로 생성하고 바닐라 CSS 스타일만 주입하여 캡처하는 100% 안심 렌더링 체계를 구축했습니다.
2. **보고서 타이틀 및 브랜드 헤더 리뉴얼**:
   - 기존의 `YYYY-MM 분임조 종합 평가 결의서` 및 상단 영문 서식(`SAMYANG INNOCHEM OFFICIAL REPORT`)을 제거하고, 요청하신 **`YYYY년 M월 분임조 종합 평가 결과서`** 정식 타이틀로 변경했습니다.
3. **정밀 데이터 테이블(Executive Office Table) 및 비율 최적화**:
   - 텍스트 나열 형식을 탈피하고 삼양 CI 블루(`#26247B`) 헤더와 격자 보더가 적용된 정밀 표 구조로 재설계했습니다.
   - `순위` 열 너비를 `6%`로 축소하고 `불합리적출` 열 너비를 `13%`로 확장(`table-layout: fixed`)하여 **'불합리적출' 헤더의 줄바꿈 현상을 완전 해결**했습니다.
4. **여백 및 프레임 밸런스 정돈**:
   - 상단 여백을 2배(`56px`)로 확장하여 시각적 답답함을 해소하고, 하단 포상 카드 영역이 테두리 선 밖으로 넘치지 않도록 깔끔하게 프레임 내부로 격리시켰습니다.

---

## [v5.0] 2026-08-05: 평가 항목 초기 기본값 '0' 설정 동기화

평가월을 변경하거나 새로운 월별 평가를 시작할 때 수동 입력 항목들이 자동으로 `0`으로 초기화되도록 동기화했습니다.

### 주요 적용 사항
1. **기본값 0 초기화 적용**:
   - **`2. 분임조`**: 계획 달성률 (`0%`), 완료 누적 건수 (`0건`)
   - **`3. 제안`**: 조원 총 불합리 적출 건수 (`0건`), 불합리 해결률 (`0%`)
2. **App.tsx 및 EvaluatorForm.tsx 상태 동기화**:
   - 신규 월 이동 및 폼 전환 시 기존 100% 기본값 부여로 인한 혼선을 방지하고 직접 수치를 기입할 수 있도록 초기화 처리 완료했습니다.
